<header-view songsData="{{songsData}}" />

<view class="song-list">
  <block wx:for="{{songsData.tracks}}" wx:key="id">
    <view class="border">
      <song-item-row songItem="{{item}}" index="{{index + 1}}" showImg="{{false}}" showIndex />
    </view>
  </block>
</view>